@model MVC_Libros.Models.Autor

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles del autor</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2 h4">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class = "col-sm-10 h4">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary">Editar</a> 
    <a asp-action="Index" class="btn btn-info">volver al listado de autores</a>
    <a asp-action="Index" asp-controller="Libros" class="btn btn-secondary">Volver al catálogo</a>
</div>
